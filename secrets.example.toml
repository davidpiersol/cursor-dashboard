# Streamlit Community Cloud: paste into the app Secrets editor (TOML).
# Copy the FULL connection string from Neon / Supabase / Railway / etc.
# Do NOT leave placeholder words like HOST, USER, PASSWORD from examples.

DATABASE_URL = "postgresql://YOUR_USER:YOUR_PASSWORD@ep-xxxx.region.provider.com:5432/your_db?sslmode=require"
