# Streamlit Community Cloud: paste into the app Secrets editor (TOML).
# Local dev: copy to .streamlit/secrets.toml (gitignored by Streamlit) or use a .env file with DATABASE_URL=...

DATABASE_URL = "postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
